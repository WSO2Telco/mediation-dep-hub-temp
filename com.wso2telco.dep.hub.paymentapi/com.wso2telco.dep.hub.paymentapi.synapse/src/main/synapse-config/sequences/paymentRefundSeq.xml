<?xml version="1.0" encoding="UTF-8"?>
<sequence name="paymentRefundSeq" trace="disable" xmlns="http://ws.apache.org/ns/synapse">
    <class name="com.wso2telco.dep.common.mediation.UtilMediator">
        <property name="propertyValue" value="singlePropertyReplacement"/>
        <property name="propertyName" value="clientCorrelator"/>
        <property name="propertyPath" value="payload.amountTransaction.clientCorrelator"/>
        <property name="msgContextProperty" value="clientCorrelator"/>
    </class>

    <property action="remove" name="REST_URL_POSTFIX" scope="axis2"/>
    <header expression="get-property('ENDPOINT')" name="To" scope="default"/>

    <property expression="json-eval($.amountTransaction.endUserId)"
              name="endUserId" scope="default" type="STRING"/>

    <call-template target="com.wso2telco.dep.common.loggingExtension.Template">
      <with-param name="direction" value="REQUESTOUT"/>
    </call-template>

    <!-- quota limit -->
    <call-template target="com.wso2telco.dep.common.quota.limit.Template">
        <with-param name="direction" value="sb"/>
    </call-template>

    <!-- data publishing -->
    <call-template target="com.wso2telco.dep.common.main.request.datapublisher.Template">
        <with-param name="msisdn" value="{$ctx:MSISDN}"/>
        <with-param name="direction" value="sb"/>
    </call-template>

    <!-- Resolve anonymize user data before operator call  -->
    <call-template target="com.wso2telco.dep.common.anonymizeUserData.Template">
        <with-param name="anonymize" value="false"/>
        <with-param name="handler" value="{$ctx:HANDLER}"/>
    </call-template>

    <call>
        <endpoint key="defaultPaymentEndpoint"/>
    </call>

    <!-- Anonymize user data  -->
    <call-template target="com.wso2telco.dep.common.anonymizeUserData.Template">
        <with-param name="anonymize" value="true"/>
        <with-param name="handler" value="{$ctx:HANDLER}"/>
    </call-template>

    <loopback/>
</sequence>
